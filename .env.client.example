# Client Environment Variables
# These variables are available in the browser and should NOT contain secrets
# IMPORTANT: Client vars are BUILD-TIME only - must be set during deployment
# They are baked into the bundle and won't update without redeploying

# Note: In Wasp, API URLs are automatically handled via WASP_SERVER_URL (server-side)
# Wasp configures the client to talk to the server automatically

# Optional: Override production API URL if needed (defaults to Railway deployment)
# Development: http://localhost:3001
# Production: https://api.nanostudioai.com
# REACT_APP_API_URL="https://api.nanostudioai.com"

# Stripe Publishable Key (safe for client-side use)
REACT_APP_STRIPE_PUBLISHABLE_KEY_TEST="pk_test_..."
REACT_APP_STRIPE_PUBLISHABLE_KEY_LIVE="pk_live_..."

# App Configuration
REACT_APP_APP_NAME="NanoStudio"
REACT_APP_SUPPORT_EMAIL="support@nanostudio.com"

# Feature Flags
REACT_APP_ENABLE_LAUNCH_OFFER="true"

# Authentication Configuration
# Set to 'true' to enable traditional email/password authentication
# Set to 'false' to only use Google OAuth
REACT_APP_ENABLE_TRADITIONAL_AUTH="false"